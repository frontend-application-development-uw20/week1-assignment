import Timer from './Timer';

// const countdown = new Timer(10);
// countdown.start();
// console.log(countdown);

// const c = () => new Timer(10);
// c.start();
// console.log(c)

const f = () => {
  const countdown = new Timer(10);
  countdown.start();
  console.log(countdown);
};
f();
